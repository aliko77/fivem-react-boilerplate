function Utils.Functions.CustomTarget.AddTargetModel(models, options, onSelect)
    --[[
        The following variables will help you.
        Integrate them according to your own target script.
        !!! return created target id ! for it to be removable
        example:
            exports.ox_target:addModel(models)
    ]]
end

function Utils.Functions.CustomTarget.RemoveTargetModel(models)
    --[[
        -- models:string or table
        example:
            exports.ox_target:removeModel(models)
    ]]
end